# פרומפט הסנכרון — עותק מקור (בלי הטוקן)

> **לא זה הקובץ שמדביקים.** להדבקה בפרויקט חדש קח את `agent-prompt-ready.local.md`
> — אותו טקסט בדיוק, עם הטוקן האמיתי בפנים, כולו מוכן להעתקה. הוא מסונן מ-git
> (`*.local.md`), ולכן הטקסט נשמר כאן כדי שלא יאבד.
>
> **כששני הקבצים משתנים — עדכן את שניהם.** זה העותק הקנוני; הקובץ המוכן נגזר ממנו.

מה שמתחת לקו הוא הפרומפט עצמו. הסוכן שמקבל אותו יגדיר לעצמו את ההנחיות
(יכתוב אותן ל-CLAUDE.md שלו), יוודא שהחיבור עובד, ומשם יעדכן לבד את המשימות
ולוג ההתקדמות בלוח הבקרה.

---

מהיום העבודה בפרויקט הזה מנוהלת בלוח הבקרה של YoniDev, שיש לו API ייעודי לסוכנים.
אני רוצה שתעדכן שם בעצמך את המשימות ואת לוג ההתקדמות, בלי שאני אצטרך לבקש.

**בצע עכשיו, לפי הסדר:**

1. שאל אותי בשאלה אחת מרוכזת את מה שחסר: `YONIDEV_URL` ו-`YONIDEV_AGENT_TOKEN`. אם הם כבר ב-`.env.local` או במשתני סביבה — קח משם ואל תשאל.
2. שמור אותם ב-`.env.local` ווודא שהוא ב-`.gitignore`. **הטוקן לא נכנס ל-git ולא ל-CLAUDE.md.**
3. הרץ `GET $YONIDEV_URL/api/agent/context` עם הטוקן, הצג לי את רשימת הפרויקטים, ושאל אותי איזה מהם זה — כדי לקבוע `PROJECT_KIND` ו-`PROJECT_ID`.
4. הוסף את הסעיף "סנכרון עם לוח הבקרה של YoniDev" שלמטה ל-`CLAUDE.md` של הפרויקט (צור את הקובץ אם אין), עם ה-`PROJECT_KIND` וה-`PROJECT_ID` שסיכמנו.
5. ממשיכים לעבוד רגיל — ומכאן אתה מעדכן את הלוח לפי הכללים.

---

## סנכרון עם לוח הבקרה של YoniDev (חובה)

העבודה בפרויקט הזה מנוהלת בלוח הבקרה של YoniDev. **אתה** אחראי לעדכן שם את המשימה
ואת לוג ההתקדמות — אל תחכה שיבקשו ממך.

### הגדרות
- `YONIDEV_URL` — כתובת הלוח (`https://yonidev.vercel.app` בפרודקשן, `http://localhost:3000` בפיתוח)
- `YONIDEV_AGENT_TOKEN` — טוקן הסוכן (ב-`.env.local` בלבד)
- `PROJECT_KIND` — `personal` או `client`
- `PROJECT_ID` — מזהה הפרויקט בלוח

כל הקריאות נושאות את הכותרת `x-agent-token: $YONIDEV_AGENT_TOKEN`.
אין login, אין cookie, אין session — טוקן אחד בכותרת וזהו.

יש בדיוק שלוש נקודות קצה:

| מתי | קריאה |
|---|---|
| בתחילת סשן | `GET /api/agent/context` — מה פתוח, מה קרה לאחרונה |
| כשמתחילים משימה | `POST /api/agent/task` — יוצר או מוצא את המשימה |
| תוך כדי ובסיום | `POST /api/agent/log` — עדכון ביומן + סטטוס + זמן + צ'קליסט |

### 1. הקשר — בתחילת כל סשן

```bash
curl -sS -H "x-agent-token: $YONIDEV_AGENT_TOKEN" \
  "$YONIDEV_URL/api/agent/context?projectKind=$PROJECT_KIND&projectId=$PROJECT_ID"
```

מחזיר את הפרויקט, הקישורים שלו, **המשימות הפתוחות עם ה-`taskKey` של כל אחת**,
הצ'קליסט של כל משימה, 10 העדכונים האחרונים, ו-`vocabulary` — רשימת הערכים
המותרים לכל שדה. אל תנחש ערכי enum; הם מגיעים בתשובה.

בלי פרמטרים הקריאה מחזירה את רשימת כל הפרויקטים והמזהים שלהם.

### 2. פתיחת משימה

```bash
curl -sS -X POST "$YONIDEV_URL/api/agent/task" \
  -H "x-agent-token: $YONIDEV_AGENT_TOKEN" -H 'Content-Type: application/json' -d '{
  "projectKind": "personal",
  "projectId": 2,
  "taskKey": "dark-mode",
  "title": "מצב כהה לאתר",
  "description": "מה בדיוק צריך לעשות",
  "type": "feature",
  "priority": "medium",
  "estimateMinutes": 120,
  "acceptance": "תנאי קבלה — מתי זה נחשב גמור",
  "steps": ["טוקנים של צבע", "מתג בהדר", "שמירה ב-localStorage"]
}'
```

- **`taskKey`** הוא הזיהוי היציב של המשימה: slug באנגלית קטנה (`dark-mode`). אתה בוחר אותו פעם אחת וכותב אותו ב-CLAUDE.md ליד המשימה. קריאה חוזרת עם אותו `taskKey` **לא** תיצור כפילות — היא תחזיר את המשימה הקיימת. לכן מותר וכדאי לקרוא לזה בפתיחת כל סשן.
- `type` / `acceptance` / `steps` קיימים רק ב-`personal`; בפרויקט לקוח הם יוחזרו כאזהרה ויתעלמו מהם.
- `status` כאן רק ליצירה ראשונית. **שינוי סטטוס של משימה קיימת נעשה דרך `/api/agent/log`** כדי שהמעבר יתועד ביומן — ה-API יזכיר לך את זה ב-`warnings`.
- התשובה כוללת `url` לדף המשימה בלוח. הצג לי אותו.

### 3. לוג התקדמות — העיקר

```bash
curl -sS -X POST "$YONIDEV_URL/api/agent/log" \
  -H "x-agent-token: $YONIDEV_AGENT_TOKEN" -H 'Content-Type: application/json' -d '{
  "projectKind": "personal",
  "projectId": 2,
  "taskKey": "dark-mode",
  "kind": "progress",
  "summary": "משפט אחד בעברית — מה נעשה",
  "details": "פירוט: קבצים שנגעתי בהם, החלטות, מה נשאר",
  "status": "in_progress",
  "nextAction": "הצעד הבא הקונקרטי",
  "completedSteps": ["טוקנים של צבע"],
  "commitSha": "abc1234",
  "timeMinutes": 45,
  "agent": "claude-code",
  "externalKey": "dark-mode-2026-07-30-01"
}'
```

כל השדות מלבד `summary` והזיהוי הם אופציונליים. בקריאה אחת נכתב עדכון ביומן, מוזז
הסטטוס, מסומנים צעדים בצ'קליסט ונרשם זמן — כדי שדיווח לא ייתקע באמצע.

- `kind`: `progress | decision | blocker | commit | research | bug | note | handoff` — בחר לפי אופי העדכון (החלטה ארכיטקטונית → `decision`, נתקעת → `blocker`, קומיט → `commit`). ערכים שאין להם מקבילה בצד הלקוח ממופים אוטומטית ל-`progress`.
- `status`: `todo | in_progress | waiting | blocked | done | canceled`. **זו הדרך היחידה להזיז משימה.** ב-`waiting` אפשר להוסיף `waitingOn`.
- `completedSteps` — כותרות מהצ'קליסט, בדיוק כפי שנוצרו. כותרת שלא נמצאה חוזרת ב-`warnings`.
- `commitSha` לבד מספיק — הלוח בונה את הקישור לגיטהאב לפי הריפו של הפרויקט.
- `timeMinutes` — זמן עבודה אמיתי בלבד, לא הערכה גסה.
- `externalKey` — מפתח ייחודי שאתה ממציא לעדכון. אם הקריאה נכשלה ואתה מנסה שוב, שלח את אותו מפתח: התשובה תחזור עם `"duplicate": true` במקום לכתוב פעמיים.

### מתי לעדכן

1. **בתחילת משימה** — `POST /api/agent/task`, ואז `log` עם `"status": "in_progress"`.
2. **תוך כדי** — `log` אחרי כל צעד משמעותי: פיצ'ר שהושלם, החלטה, באג שנפתר, חסימה, קומיט.
3. **בסיום** — `log` עם סיכום, `"status": "done"`, ו-`nextAction` אם נשאר משהו פתוח.

**אל תשאל רשות לפני כל עדכון** — פשוט עשה, וציין בשורה אחת מה נכתב ולאיזה `url`.

### כללים

- כתוב `summary` / `details` / `nextAction` **בעברית**, ענייני, בלי שיווק.
- `summary` = שורה אחת שאפשר לסרוק ברשימה (עד 500 תווים). `details` = ההקשר המלא.
- `taskKey` תמיד באנגלית קטנה עם מקפים. שמור אותו ב-CLAUDE.md ליד תיאור המשימה.
- אל תמציא `projectId`. אם אתה לא בטוח — `GET /api/agent/context` בלי פרמטרים.
- אם קריאה מחזירה שגיאה, אל תנסה שוב בלולאה: 401 = טוקן שגוי, 404 = המשימה לא קיימת בפרויקט הזה, 422 = שדה לא תקין (הפירוט ב-`issues`). דווח לי ותמשיך לעבוד.
- אם הלוח לא זמין — המשך לעבוד רגיל וציין בסוף שהעדכון לא נשמר.
- ה-API הזה לא יכול למחוק כלום ולא נוגע בלקוחות, בחשבוניות או בתקשורת מול לקוח. אם נראה לך שצריך משהו כזה — תגיד לי, אני אעשה את זה בלוח.
